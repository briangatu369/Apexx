{
  "name": "apexx",
  "version": "1.0.0",
  "description": "Real-time crash game where players bet, watch the multiplier rise, and cash out before it crashes.",
  "main": "server.js",
  "scripts": {
    "start": "node ./dist/server.js",
    "build": "tsc",
    "dev": "nodemon --exec ts-node src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brian Gatundu",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.13.1",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "mongoose": "^8.10.0"
  }
}
